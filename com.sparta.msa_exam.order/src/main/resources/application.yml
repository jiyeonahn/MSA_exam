spring:
  profiles:
    default: dev
  application:
    name: order-service
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
        highlight_sql: true
server:
  port: 19092
management:
  tracing:
    sampling:
      probability: 1.0